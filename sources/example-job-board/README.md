# Example Job Board Source Plugin

This folder provides a reference template for writing community opportunity connectors for **Opportunity Radar**.

## Configuration

If your source requires API credentials, add them to your `.env` file:

```bash
EXAMPLE_JOB_BOARD_API_KEY="your-api-key"
